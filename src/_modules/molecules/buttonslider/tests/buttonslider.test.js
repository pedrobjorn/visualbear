'use strict';

import ButtonSlider from '../button-slider';

describe('ButtonSlider View', function() {

  beforeEach(() => {
    this.buttonSlider = new ButtonSlider();
  });

  it('Should run a few assertions', () => {
    expect(this.buttonSlider);
  });

});
