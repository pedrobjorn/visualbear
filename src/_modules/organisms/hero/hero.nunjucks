{% macro content(spec) %}
{% set spec = spec or {} %}
{% set _class = spec.class or '' %}
{% set _content = spec.content or '' %}
<div class="hero {{_class}}">
	<div class="content">
		<div class="jumbo-text">
			{{_content.title}}
		</div>
	</div>
</div>
{% endmacro %}