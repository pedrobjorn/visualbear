{% macro content(spec) %}
{% set spec = spec or {} %}
{% set _class = spec.class or '' %}
{% set _content = spec.content or '' %}
<div class="hero {{_class}}">
	<div class="content">
		<b>{{_content.title}}</b>
		<div>{{_content.body}}</div>
	</div>
</div>
{% endmacro %}