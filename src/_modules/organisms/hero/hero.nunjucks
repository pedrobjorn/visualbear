{% macro content(spec) %}
{% set spec = spec or {} %}
{% set _class = spec.class or '' %}
{% set _content = spec.content or '' %}
<div class="hero {{_class}}">
	<div class="content">
		<title>{{_content.title}}</title>
		<subtitle>{{_content.body}}</subtitle>
	</div>
</div>
{% endmacro %}